/*
Compléter la fonction display pour qu'elle affiche le contenu du tableau sur la sortie standard. Chaque valeur sera séparée par un espace.

Exemple : le tableau contenant les valeurs 1,2,3 et 4 devra être affiché "1 2 3 4"
*/
void display(int* tab, int taille) {
  for (int i =​‍‌‍﻿﻿﻿‌﻿ 0; i < taille; i++)
    printf("%d ", tab[i]);
}
